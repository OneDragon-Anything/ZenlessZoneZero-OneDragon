---
screen_name: 地图
appears_in: [大世界]
last_updated: 2026-07-15
source_image: 选传送点
status: 选传送点 / 传送确认弹窗子态已建(2026-07-15 实拍);区域选择 default 态待补
---

# 地图

## 何时出现 + 状态流转

大世界**传送流程**(`MapTransport`)用的**网格/列表传送视图**(用户称「网格选择」)。区别于 [3D地图](3D地图.md)(立体俯视,`world_patrol`/锄大地用):本画面是 2D 列表式 —— 底部传送点列表 + 中央确认弹窗,**传送走「确认」按钮**(3D地图走「前往」按钮,无确认弹窗)。

| 子态 | 入口 | 出口 |
|---|---|---|
| ① 选传送点(列表) | 选区域后底部出传送点列表(随便观/阿朔/汀曼咖啡…) | 点传送点 → ② |
| ② 传送确认弹窗 | 点传送点 → 弹「是否传送至该地点？」(确认/取消) | 点「确认」(`地图/确认` area)→ 传送落地;取消 → 回① |

> `MapTransport` 流程(`src/zzz_od/operation/map_transport.py`):选区域(`choose_area`,整屏 OCR 区域名 + 横向 drag)→ 选传送点(`choose_tp`,OCR `地图/传送点名称` 列表)→ 点确认(`click_tp`,`地图/确认`)。区域选择无独立 area(整屏 OCR),区域 default 态截图待补。

## 识别特征(稳定锚点)

- **传送点名称** area(@70,1000-1848,1058,底部传送点列表,无 text)。
- **确认** area(@1024,600-1232,708,text`确认`,弹窗绿√按钮)。
- **左上角返回**(template `menu/back` @82,13-150,90)。
- **易变值**:传送点名称 / 当前选择(**动态**)。

## 可交互元素

- 传送点列表(点传送点 → 弹确认)。
- 确认 / 取消(确认弹窗)。
- 左上返回。

## 识别快照

### 1. 选传送点态(`选传送点.webp`,2026-07-15 实拍)

点区域(澄辉坪)后底部出传送点列表。OCR:随便观 / 阿朔 / 汀曼咖啡 / 录像店营业点 / 饮茶仙 / 临界推演(`传送点名称` area @70,1000-1848,1058)。点传送点 → ②。

### 2. 传送确认弹窗态(`传送确认弹窗.webp`,2026-07-15 实拍)

点传送点后弹「是否传送至该地点？」+ 确认(绿√ @1101,609,`地图/确认` area @1024,600-1232,708)+ 取消(红× @798,610)。点确认 → 传送落地。

## 备注

- **screen_info**:`map.yml`(3 area:传送点名称 / 确认 / 左上角返回)。
- **与 3D地图 区别**:本画面 = 网格/列表传送(`MapTransport`,确认按钮);[3D地图](3D地图.md) = 立体俯视(`world_patrol`/`TransportBy3dMap`,图标 + 前往按钮,无确认弹窗)。两者 screen_info 独立(`map.yml` vs `3d_map.yml`)。
- **入口**:大世界传送流程打开(`Transport` op;区域选择 default 态待补)。
